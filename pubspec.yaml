name: sfit_step_counter
description: "Step Counter is a lightweight Flutter package that detects steps in real-time using the accelerometer via sensors_plus."
version: 0.0.13
homepage: https://github.com/dennismuhia/sfit_step_Counter.git


environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^6.1.0
  flutter_background: ^1.3.0
  shared_preferences: ^2.5.2
  # step_counter:
  #  path: ../step_counter  # adjust path as needed 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
